# Shopify Portfolio

A simple, static portfolio site for George Vinarao, focused on Shopify e-commerce support services.

## How to view locally

Open `index.html` in a browser, or run a small local server:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
